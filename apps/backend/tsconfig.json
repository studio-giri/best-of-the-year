{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		// Environment-specific: server target, no DOM lib, allow JS sources
		"lib": [
			"ESNext"
		],
		"target": "ESNext",
		"module": "Preserve",
		"allowJs": true
	}
}
