{
	"extends": "../../tsconfig.base.json",
	"include": [
		"src"
	],
	"compilerOptions": {
		"target": "ESNext",
		"module": "ESNext",
		// Bun's test runner (`bun:test`) types for colocated *.test.ts files
		"types": [
			"bun"
		]
	}
}
